/*import styled, { css } from "styled-components";
import Link from "next/link";

const StyledLink = styled(Link)`
  background-color: ${({ $variant }) => };
`;

export default function NextLink( {}) {
  return <StyledLink $variant={$variant}></StyledLink>;
}*/
